Version 1/190315 of the Entity Only Language by TextWorld begins here.

Part 1 - Overwriting TextWorld stuff

Instead of examining open container (called target):
	say "[list of things in the target]";

Instead of examining supporter (called target):
	say "[list of things on the target]";

Report opening container (called target):
	say "You open [the target]." instead.

The Fake Language end here.